Copy `config.firebase.example.js` to `config.firebase.js` and insert your Firebase web app config.
Do not commit `config.firebase.js` if you want to keep environment-specific values out of the repository.
